import com.justsy.mdm.constant.ConstantAlarm;
import com.justsy.mdm.manage.general.SaveManageData;


public class asd {

    /**
     * @param args
     */
    public static void main(String[] args) {
        Long currenttime = System.currentTimeMillis();
        Long lasttime = System.currentTimeMillis()-60000;
        long o=1000*60*60*24*30;
        if (lasttime != 0) {
            if ((currenttime - lasttime) >= (1000*60*60*24*30) {
    }

}
